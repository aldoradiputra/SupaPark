import { PagePlaceholder } from "@/components/placeholder";

export default function MembersPage() {
  return (
    <PagePlaceholder
      title="Members"
      description="Location-scoped memberships and validity."
    />
  );
}
