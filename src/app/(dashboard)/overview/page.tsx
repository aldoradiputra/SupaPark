import { PagePlaceholder } from "@/components/placeholder";

export default function OverviewPage() {
  return (
    <PagePlaceholder
      title="Overview"
      description="Live occupancy, today's revenue, active sessions and alerts."
    />
  );
}
