import { PagePlaceholder } from "@/components/placeholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Deployment pipeline from lead to live."
    />
  );
}
