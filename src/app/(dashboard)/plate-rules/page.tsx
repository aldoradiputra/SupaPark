import { PagePlaceholder } from "@/components/placeholder";

export default function PlateRulesPage() {
  return (
    <PagePlaceholder
      title="Plate Rules"
      description="Whitelist, blacklist and VIP plates per location."
    />
  );
}
