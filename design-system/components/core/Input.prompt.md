**Input** — single-line text field. Raised surface, hairline border, amber focus ring.

```jsx
<Input placeholder="Cari plat nomor…" />
<Input type="email" defaultValue="aldo@supapark.id" />
<Input disabled placeholder="Terkunci" />
```

40px tall, 8px radius. Pair with a `12px/500` label above it.
